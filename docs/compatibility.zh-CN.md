# 兼容性

计划发布的客户端支持 Python 3.9-3.13 与本地 Codex 状态。归档、报表和回环地址浏览器支持跨
平台；本地调度在 macOS 使用 launchd，在 Windows 使用 Task Scheduler。iCloud 加密快照仅支持
macOS。每个正式版本以其附带的验收证据为准。

在签名的 macOS 与 Windows 客户端通过干净主机验收前，公开目录会保持为空。
