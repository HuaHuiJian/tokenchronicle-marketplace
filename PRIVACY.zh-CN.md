# TokenChronicle 词元日志隐私说明

TokenChronicle 采用本地优先设计。干净程序包不包含任何用户会话、项目路径、设置、凭证、报表或历史归档。

## 默认边界

- 程序仅以只读方式读取用户选择的 Codex 数据源，不修改 Codex 核心文件。
- 用户输入、Codex 回复、过程记录和索引默认保存在用户自己的应用数据目录。
- Web 浏览器默认只监听 `127.0.0.1`，不向局域网或互联网开放。
- 安装后不会自动创建每日 Automation，因此默认每日模型 Token 消耗为 0。
- 原始事件不会复制到发布包；只有用户明确执行证据导出时才生成相应文件。

## 可选反馈

反馈草稿默认只保存在本地。联网发送必须由用户启用独立安装实例凭证，并在每次发送前确认预览内容。反馈载荷不允许携带会话正文、thread id、项目路径或归档文件。用户可以随时停用反馈凭证。

## 语言边界

界面和产品指南支持简体中文与英文。会话标题、用户输入、Codex 回复、过程证据和归档文件始终忠实保留原文，不进行自动翻译或改写。
