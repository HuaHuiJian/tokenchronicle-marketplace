# TokenChronicle 词元日志隐私说明

TokenChronicle 采用本地优先设计。干净程序包不包含任何用户会话、项目路径、设置、凭证、报表或历史归档。

## 默认边界

- 程序仅以只读方式读取用户选择的 Codex 数据源，不修改 Codex 核心文件。
- 用户输入、Codex 回复、过程记录和索引默认保存在用户自己的应用数据目录。
- Web 浏览器默认只监听 `127.0.0.1`，不向局域网或互联网开放。
- 安装后不会自动创建每日 Automation，因此默认每日模型 Token 消耗为 0。
- 原始事件不会复制到发布包；只有用户明确执行证据导出时才生成相应文件。

## 目录与用户选择

- Codex 插件代码与用户数据分离。插件运行时优先使用 Codex 提供的 `PLUGIN_DATA` 可写目录；
  独立安装使用操作系统的用户级应用数据目录。
- 配置、锁、日志、反馈和凭证属于运行状态，默认不放在 `Documents`。
- 会话档案、报表和记忆属于长期档案库。用户可以保留默认私有位置，也可以明确选择
  `Documents/TokenChronicle` 等便于查看和迁移的位置。
- `Documents` 可能被 iCloud、企业同步、搜索索引或备份软件处理。选择目录不会自动开启本地
  调度、Codex Automation、反馈传输或历史迁移。

## 可选反馈

反馈草稿默认只保存在本地。联网发送必须由用户启用独立安装实例凭证，并在每次发送前确认预览内容。反馈载荷不允许携带会话正文、thread id、项目路径或归档文件。用户可以随时停用反馈凭证。

## 可选 iCloud 加密快照

- iCloud 备份默认关闭，只能由用户手工确认后创建。
- 实时档案和运行状态始终留在本地；上传的是本地完成 AES-256 加密并验证后的只读 DMG 快照。
- 密码只在用户终端交互输入，经标准输入传递给 macOS 系统工具，不保存、不记录、不上传。
- 快照内部包含逐文件大小和 SHA-256 清单；外部回执只保留汇总、镜像校验和与验证状态。
- iCloud 是同步服务，不是独立备份，建议同时保留其他备份。

## 语言边界

界面和产品指南支持简体中文与英文。会话标题、用户输入、Codex 回复、过程证据和归档文件始终忠实保留原文，不进行自动翻译或改写。

## 联系方式

- 隐私与数据权利请求：`privacy@h2me.tech`
- 普通产品支持：`service@h2me.tech`
- 保密安全漏洞报告：`security@h2me.tech`
- 通信地址：天津市滨海新区中新生态城启发大厦，邮编 300467
